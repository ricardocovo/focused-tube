name: focused-tube
metadata:
  template: focused-tube
services:
  client:
    project: ./client
    dist: dist
    language: js
    host: staticwebapp
  server:
    project: ./server
    language: js
    host: appservice
